{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install",
  "buildCommand": "npm run build && rm -rf dist && mkdir -p dist && cp -r ../Frontend/dist/* dist/",
  "outputDirectory": "dist"
}
